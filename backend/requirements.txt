# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
# gunicorn>=21.2.0 # Not needed for Vercel Serverless (uses its own runner)

# Database
sqlalchemy>=2.0.25
alembic>=1.13.1
psycopg2-binary>=2.9.11

# Validation & Settings
pydantic>=2.5.3
pydantic-settings>=2.1.0
python-dotenv>=1.0.1
email-validator>=2.1.0

# Auth & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6

# HTTP & AI
requests>=2.31.0
httpx>=0.23.0
openai>=1.10.0
# azure-cognitiveservices-speech>=1.34.0 # HEAVY (>80MB). Try deploying without this first.

# Utilities
aiofiles>=23.2.1
slowapi>=0.1.9
jinja2>=3.1.4

# Lighter Parsing Alternatives
pypdf>=4.0.0
chardet>=5.2.0
langdetect>=1.0.9

# REMOVED FOR SIZE OPTIMIZATION (VERCEL LIMIT)
# pdfplumber>=0.10.3  # Use pypdf instead
# pillow>=10.1.0      # Too heavy, OCR not supported on Vercel
# python-docx>=1.1.0  # Removing docx support for now
# emails>=0.6         # Use httpx/requests for email APIs
